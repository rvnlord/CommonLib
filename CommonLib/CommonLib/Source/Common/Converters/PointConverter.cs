namespace CommonLib.Source.Common.Converters
{
    public static class PointConverter
    {
        //public static Point ToPoint(this DPoint p) => new Point(p.X, p.Y);
        //public static DPoint ToDrawingPoint(this Point p) => new DPoint(p.X.ToInt(), p.Y.ToInt());
    }
}
